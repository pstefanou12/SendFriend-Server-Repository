port = 8000

[database]
type = "mysql"
dsn = "root:25503885@/Users"

[queries]
federation = "SELECT 'GAQHYET7SRBVMVONSMT2TUWO37YISDYZ3SNDONQ66TNLI5OCXEMJSILH' as id, 'hash' as memo_type, user_id as memo from Users WHERE username = ? AND domain = ?"

[tls]
certificate-file = "/etc/letsencrypt/live/sendfriend.io/fullchain.pem"
private-key-file = "/etc/letsencrypt/live/sendfriend.io/privkey.pem"
